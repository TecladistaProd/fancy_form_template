{
  "name": "fancy-form",
  "version": "0.1.0",
  "description": "a study project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w src/sass/ -o src/css/",
    "server": "concurrently --kill-others-on-fail \"yarn sass\" \"http-server --port=8080\""
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "keywords": [
    "."
  ],
  "author": "TecladistapOrd",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.6.0",
    "node-sass": "^4.9.2"
  }
}
